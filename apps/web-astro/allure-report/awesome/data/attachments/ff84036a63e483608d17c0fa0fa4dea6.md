# Instructions

- Following Playwright test failed.
- Explain why, be concise, respect Playwright best practices.
- Provide a snippet of code with the fix, if possible.

# Test info

- Name: tests/features/admin/editor.feature.spec.js >> Admin Article Editor >> Admin creates a new article
- Location: .features-gen/tests/features/admin/editor.feature.spec.js:6:3

# Error details

```
Test timeout of 30000ms exceeded.
```

```
Error: page.goto: Test timeout of 30000ms exceeded.
Call log:
  - navigating to "http://localhost:4321/admin/artigos/new", waiting until "load"

```

# Page snapshot

```yaml
- generic [active] [ref=e1]: Internal server error
```

# Test source

```ts
  1  | import { Given, When, Then, expect } from "../fixtures/base.fixture";
  2  |
  3  | Given("the user is on the homepage", async ({ page }) => {
  4  |   await page.goto("/");
  5  | });
  6  |
  7  | Given("the user navigates to {string}", async ({ page }, url: string) => {
  8  |   await page.goto(url);
  9  | });
  10 |
  11 | When("they navigate to {string}", async ({ page }, url: string) => {
> 12 |   await page.goto(url);
     |              ^ Error: page.goto: Test timeout of 30000ms exceeded.
  13 | });
  14 |
  15 | Given("the user is logged in as admin", async ({ page }) => {
  16 |   // TODO: Implement actual login logic once auth is migrated
  17 |   await page.goto("/admin/login");
  18 | });
  19 |
  20 | Then(
  21 |   "the page title should contain {string}",
  22 |   async ({ page }, title: string) => {
  23 |     await expect(page).toHaveTitle(new RegExp(title));
  24 |   }
  25 | );
  26 |
```
