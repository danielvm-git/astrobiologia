# Instructions

- Following Playwright test failed.
- Explain why, be concise, respect Playwright best practices.
- Provide a snippet of code with the fix, if possible.

# Test info

- Name: tests/features/categories.feature.spec.js >> Categories >> User browses a category
- Location: .features-gen/tests/features/categories.feature.spec.js:6:3

# Error details

```
Error: expect(locator).toContainText(expected) failed

Locator: locator('h1')
Expected pattern: /noticias/i
Received string:  "404:  Not found"
Timeout: 5000ms

Call log:
  - Expect "toContainText" with timeout 5000ms
  - waiting for locator('h1')
    9 × locator resolved to <h1>…</h1>
      - unexpected value "404:  Not found"

```

# Page snapshot

```yaml
- main [ref=e2]:
    - img [ref=e3]
    - 'heading "404: Not found" [level=1] [ref=e7]'
    - generic [ref=e8]: "Path: /categorias/noticias"
```

# Test source

```ts
  1  | import { Then, expect } from "../fixtures/base.fixture";
  2  |
  3  | Then(
  4  |   "they should see articles for the {string} category",
  5  |   async ({ page }, category: string) => {
  6  |     // Check for some category indicator in the UI
> 7  |     await expect(page.locator("h1")).toContainText(new RegExp(category, "i"));
     |                                      ^ Error: expect(locator).toContainText(expected) failed
  8  |   }
  9  | );
  10 |
```
