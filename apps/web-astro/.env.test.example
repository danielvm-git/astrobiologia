# Copy to `.env.test` and fill in real values (gitignored).
# Repo-root `.env` is also loaded; these keys merge without overwriting existing shell env.

E2E_ADMIN_EMAIL=admin@example.com
E2E_ADMIN_PASSWORD=your_admin_password

# Aliases (optional if E2E_ADMIN_* set above)
TEST_USER_EMAIL=admin@example.com
TEST_USER_PASSWORD=your_admin_password
