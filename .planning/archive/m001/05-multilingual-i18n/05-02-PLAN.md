---
phase: "05"
plan: "02"
type: execute
wave: 2
depends_on: ["05-01"]
files_modified: ["src/lib/appwrite.ts", "scripts/migrate-articles-i18n.js"]
autonomous: false
requirements: ["I18N-03", "I18N-06"]
must_haves:
  truths:
    - "New article_translations collection exists in Appwrite"
    - "Articles collection schema is updated to separate shared metadata"
    - "Existing articles are successfully migrated to the relational structure"
  artifacts:
    - path: "scripts/migrate-articles-i18n.js"
      provides: "Migration logic for converting articles to relational structure"
  key_links:
    - from: "src/lib/appwrite.ts"
      to: "article_translations"
      via: "Databases helper functions (list, create, update)"
---

<objective>
Refactor the Appwrite database schema to a relational translation pattern and migrate existing content.

Purpose: Enable scalable multi-language support for articles without duplicating shared metadata.
Output: Updated database schema, migration script, and refactored Appwrite helpers.
</objective>

<execution_context>
@/Users/me/Sites/astrobiologia/.gemini/get-shit-done/workflows/execute-plan.md
@/Users/me/Sites/astrobiologia/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-multilingual-i18n/05-RESEARCH.md
@src/lib/appwrite.ts
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Appwrite Schema Preparation</what-built>
  <how-to-verify>
    1. Log in to Appwrite Console.
    2. Create a new collection named `article_translations`.
    3. Add attributes: `article_id` (string/relationship), `language` (string), `title` (string), `slug` (string), `excerpt` (string/text), `content` (string/text), `metaTitle` (string), `metaDescription` (string).
    4. Create an index on `slug` (unique) and `article_id` + `language` (unique).
    5. Update `articles` collection to remove `title`, `slug`, `excerpt`, `content`, `language`, `metaTitle`, `metaDescription` (after migration).
  </how-to-verify>
  <resume-signal>Collection created and attributes configured</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Implement and Run Migration Script</name>
  <files>scripts/migrate-articles-i18n.js</files>
  <action>
    - Create a script that:
      1. Fetches all articles from the current `articles` collection.
      2. For each article, creates a entry in `article_translations` (default language 'pt-br').
      3. Verifies data integrity.
    - Run the script and confirm success.
    - Commit: `feat(db): migrate existing articles to relational translation pattern`
  </action>
  <verify>
    <automated>node scripts/migrate-articles-i18n.js --dry-run</automated>
  </verify>
  <done>Existing articles are safely moved to the new relational structure.</done>
</task>

<task type="auto">
  <name>Task 3: Update Appwrite Helpers for Relational Fetch</name>
  <files>src/lib/appwrite.ts</files>
  <action>
    - Update `Article` and `ArticleTranslation` interfaces.
    - Refactor `getArticleBySlug` to first find the translation, then join with the master article.
    - Update `getPublishedArticles` to join master metadata with the requested language's translation.
    - Commit: `refactor(lib): update appwrite helpers for relational translations`
  </action>
  <verify>
    <automated>pnpm check</automated>
  </verify>
  <done>Appwrite helpers correctly handle fetching articles via the new relational schema.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary  | Description                                                          |
| --------- | -------------------------------------------------------------------- |
| API -> DB | Secure database operations using server keys or authorized sessions. |

## STRIDE Threat Register

| Threat ID | Category  | Component    | Disposition | Mitigation Plan                                                             |
| --------- | --------- | ------------ | ----------- | --------------------------------------------------------------------------- |
| T-05-03   | Data Loss | migration.js | mitigate    | Script includes a --dry-run mode and performs verification before deletion. |
| T-05-04   | Injection | appwrite.ts  | mitigate    | Appwrite SDK handles parameter sanitization for queries.                    |

</threat_model>

<verification>
- Run migration script.
- Verify `article_translations` collection is populated via Appwrite console.
</verification>

<success_criteria>
The database schema is migrated, and the backend helpers can retrieve localized articles using the new structure.
</success_criteria>

<output>
After completion, create `.planning/phases/05-multilingual-i18n/05-02-SUMMARY.md`
</output>
