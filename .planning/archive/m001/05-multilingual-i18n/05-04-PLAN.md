---
phase: "05"
plan: "04"
type: execute
wave: 3
depends_on: ["05-02"]
files_modified:
  [
    "src/lib/components/ArticleEditor.svelte",
    "src/routes/admin/artigos/[id]/edit/+page.server.ts",
    "src/routes/api/translate/+server.ts",
    "src/lib/appwrite.ts",
  ]
autonomous: true
requirements: ["I18N-05", "I18N-08"]
user_setup:
  - service: DeepL
    why: "Automatic draft translations for articles"
    env_vars:
      - name: DEEPL_AUTH_KEY
        source: "DeepL Account Settings -> API Keys"
must_haves:
  truths:
    - "Article editor supports selecting and editing multiple languages"
    - "DeepL translations can be triggered for article content"
    - "New translations are saved correctly in the relational schema"
  artifacts:
    - path: "src/routes/api/translate/+server.ts"
      provides: "Server-side endpoint for DeepL translation"
  key_links:
    - from: "src/lib/components/ArticleEditor.svelte"
      to: "src/routes/api/translate/+server.ts"
      via: "fetch call to trigger translation"
---

<objective>
Update the Admin CMS to support multiple languages and integrate DeepL for automated draft translations.

Purpose: Streamline the creation of multilingual content and provide high-quality draft translations for the editorial team.
Output: Updated ArticleEditor with multi-language support and working DeepL integration.
</objective>

<execution_context>
@/Users/me/Sites/astrobiologia/.gemini/get-shit-done/workflows/execute-plan.md
@/Users/me/Sites/astrobiologia/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-multilingual-i18n/05-RESEARCH.md
@src/lib/components/ArticleEditor.svelte
@src/lib/appwrite.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update ArticleEditor for Multi-language Support</name>
  <files>src/lib/components/ArticleEditor.svelte, src/lib/appwrite.ts</files>
  <action>
    - Update `ArticleEditor.svelte` to add a language selector/tab system.
    - Modify state management to track translations for each language.
    - Ensure shared metadata (image, category) is kept in sync across translations.
    - Update the `onSave` logic to handle saving both the master article and its translations.
    - Commit: `feat(cms): update article editor for multi-language support`
  </action>
  <verify>
    <automated>pnpm check</automated>
  </verify>
  <done>ArticleEditor is updated to allow editing different language versions of the same article.</done>
</task>

<task type="auto">
  <name>Task 2: Implement DeepL Translation Server Action</name>
  <files>src/routes/api/translate/+server.ts, src/lib/components/ArticleEditor.svelte</files>
  <action>
    - Install `deepl-node`.
    - Create a server-side route `src/routes/api/translate/+server.ts` that uses `deepl-node` to translate content.
    - Add a "Translate with DeepL" button to the `ArticleEditor.svelte` UI.
    - Implement the client-side logic to fetch the translation and update the editor state (preserving Tiptap HTML structure).
    - Commit: `feat(cms): integrate DeepL translation for article drafts`
  </action>
  <verify>
    <automated>pnpm check</automated>
  </verify>
  <done>DeepL translation is implemented and available in the article editor.</done>
</task>

<task type="auto">
  <name>Task 3: Final Verification and UAT</name>
  <files>tests/phase5/i18n.test.ts</files>
  <action>
    - Create a comprehensive verification test covering the full translation flow:
      1. Create master article.
      2. Add English translation.
      3. Verify both are saved and linked.
      4. Verify public routing works for both.
    - Commit: `test(i18n): add comprehensive translation flow test`
  </action>
  <verify>
    <automated>pnpm test tests/phase5/i18n.test.ts</automated>
  </verify>
  <done>Phase 5 is verified and ready for production.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary     | Description                                            |
| ------------ | ------------------------------------------------------ |
| API -> DeepL | Secure communication using API Key on the server side. |

## STRIDE Threat Register

| Threat ID | Category               | Component            | Disposition | Mitigation Plan                                                                 |
| --------- | ---------------------- | -------------------- | ----------- | ------------------------------------------------------------------------------- |
| T-05-06   | Information Disclosure | DeepL Key            | mitigate    | Key is stored in environment variables and never exposed to the client.         |
| T-05-07   | DoS                    | translate/+server.ts | mitigate    | Endpoint is protected by admin authentication (guaranteed by Appwrite session). |

</threat_model>

<verification>
- Test creating a new article in Portuguese.
- Use DeepL to translate it to English.
- Save and verify both versions exist in the database and are accessible via their respective URLs.
</verification>

<success_criteria>
Admin CMS fully supports the creation, translation (via DeepL), and management of multilingual articles.
</success_criteria>

<output>
After completion, create `.planning/phases/05-multilingual-i18n/05-04-SUMMARY.md`
</output>
