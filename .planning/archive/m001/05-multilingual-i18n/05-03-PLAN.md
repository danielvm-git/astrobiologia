---
phase: "05"
plan: "03"
type: execute
wave: 3
depends_on: ["05-02"]
files_modified:
  [
    "src/lib/components/LanguageSwitcher.svelte",
    "src/lib/components/Header.svelte",
    "src/lib/components/Footer.svelte",
    "src/routes/+layout.svelte",
    "src/routes/artigos/[slug]/+page.svelte",
  ]
autonomous: true
requirements: ["I18N-04", "I18N-07"]
must_haves:
  truths:
    - "Language switcher is visible and functional in Header and Footer"
    - "Public pages (Home, Article) display content in the correct language"
    - "SEO metadata (hreflang, og:locale) is correctly set based on current language"
  artifacts:
    - path: "src/lib/components/LanguageSwitcher.svelte"
      provides: "Svelte 5 component for switching between PT-BR and EN"
  key_links:
    - from: "src/routes/+layout.svelte"
      to: "src/lib/components/LanguageSwitcher.svelte"
      via: "Component import and inclusion"
---

<objective>
Localize the public portal's UI, implement a language switcher, and ensure SEO compliance.

Purpose: Provide a seamless multilingual user experience and maintain search engine visibility for all languages.
Output: Working language switcher, translated UI, and localized SEO tags.
</objective>

<execution_context>
@/Users/me/Sites/astrobiologia/.gemini/get-shit-done/workflows/execute-plan.md
@/Users/me/Sites/astrobiologia/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-multilingual-i18n/05-RESEARCH.md
@src/lib/components/Header.svelte
@src/lib/components/Footer.svelte
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement LanguageSwitcher Component</name>
  <files>src/lib/components/LanguageSwitcher.svelte</files>
  <action>
    - Create `LanguageSwitcher.svelte` using Svelte 5 runes.
    - Use `paraglide-sveltekit`'s `i18n` instance for switching locales and resolving routes.
    - Ensure it uses the "New Scientist" premium aesthetic (sleek, minimal).
    - Avoid flags; use Native language names (Português, English).
    - Commit: `feat(ui): implement language switcher component`
  </action>
  <verify>
    <automated>pnpm check</automated>
  </verify>
  <done>The LanguageSwitcher is implemented and functional.</done>
</task>

<task type="auto">
  <name>Task 2: Integrate Localized Navigation and UI</name>
  <files>src/lib/components/Header.svelte, src/lib/components/Footer.svelte, src/routes/+layout.svelte</files>
  <action>
    - Import and add `LanguageSwitcher` to `Header.svelte` and `Footer.svelte`.
    - Replace static text strings in Header, Footer, and Root Layout with paraglide message calls (e.g., `m.nav_home()`).
    - Update navigation links to use localized paths via paraglide.
    - Commit: `feat(ui): integrate language switcher and localized UI messages`
  </action>
  <verify>
    <automated>pnpm check</automated>
  </verify>
  <done>UI elements are translated and the switcher is integrated into the site layout.</done>
</task>

<task type="auto">
  <name>Task 3: Implement SEO hreflang and Metadata</name>
  <files>src/routes/+layout.svelte, src/lib/seo.ts</files>
  <action>
    - Inject `<link rel="alternate" hreflang="...">` tags for all supported languages in the root layout's `<svelte:head>`.
    - Update `src/lib/seo.ts` (if it exists) to handle localized `og:locale` and meta tags.
    - Ensure `lang` attribute on the `<html>` tag is correctly set.
    - Commit: `feat(seo): implement localized hreflang and meta tags`
  </action>
  <verify>
    <automated>pnpm check</automated>
  </verify>
  <done>SEO metadata correctly reflects the current language and provides alternate versions for engines.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary     | Description                                            |
| ------------ | ------------------------------------------------------ |
| Client -> UI | UI displays content based on the current locale state. |

## STRIDE Threat Register

| Threat ID | Category               | Component | Disposition | Mitigation Plan                                                           |
| --------- | ---------------------- | --------- | ----------- | ------------------------------------------------------------------------- |
| T-05-05   | Information Disclosure | Metadata  | mitigate    | Ensure canonical and alternate links only point to valid, existing pages. |

</threat_model>

<verification>
- Switch between Portuguese and English via the UI.
- Inspect the source code to verify `<link rel="alternate" hreflang="...">` tags.
</verification>

<success_criteria>
The public portal is fully localized, including UI, content, and SEO metadata.
</success_criteria>

<output>
After completion, create `.planning/phases/05-multilingual-i18n/05-03-SUMMARY.md`
</output>
