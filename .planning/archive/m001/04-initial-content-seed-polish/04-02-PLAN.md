---
phase: 04-initial-content-seed-polish
plan: 02
type: execute
wave: 2
depends_on: ["04-01"]
files_modified: [src/lib/components/ArticleCard.svelte, src/routes/artigos/[slug]/+page.svelte, src/app.html, .env.example]
autonomous: false
requirements: [FR4.1, NFR1.2, NFR1.4]
user_setup:
  - service: Appwrite
    why: "Deployment on Appwrite Sites"
    dashboard_config:
      - task: "Configure Appwrite Sites for the project"
        location: "Appwrite Dashboard -> Settings -> Sites"

must_haves:
  truths:
    - "Images are lazily loaded and optimized for speed"
    - "Site is accessible via the public Appwrite Sites URL"
    - "Lighthouse performance score is 90+"
    - "SEO meta tags are correctly rendered in production"
  artifacts:
    - path: "src/app.html"
      provides: "Final HTML structure with SEO meta"
    - path: "src/lib/components/ArticleCard.svelte"
      provides: "Optimized image delivery"
  key_links:
    - from: "Appwrite Storage"
      to: "Browser"
      via: "Appwrite Image Transformation API"
---

<objective>
Finalize deployment on Appwrite Sites and optimize the portal for performance and SEO.

Purpose: Ship the MVP to a public URL with excellent performance.
Output: Optimized asset delivery and a live production site.
</objective>

<execution_context>
@/Users/me/Sites/astrobiologia/.gemini/get-shit-done/workflows/execute-plan.md
@/Users/me/Sites/astrobiologia/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-initial-content-seed-polish/04-CONTEXT.md
@src/lib/appwrite.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement Image Optimization</name>
  <files>src/lib/components/ArticleCard.svelte, src/routes/artigos/[slug]/+page.svelte</files>
  <action>
    - Update `ArticleCard.svelte` to use Appwrite's image transformation URL parameters (`width`, `height`, `quality=80`, `output=webp`).
    - Implement `loading="lazy"` on all article images that are below the fold.
    - Add `fetchpriority="high"` to the Hero image on the home page.
    - Ensure `alt` text is correctly pulled from the article metadata for accessibility.
  </action>
  <verify>
    <automated>pnpm build</automated>
  </verify>
  <done>Images are delivered in optimized formats (WebP) with appropriate sizing.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Deployment Configuration</what-built>
  <how-to-verify>
    - Go to the Appwrite Dashboard.
    - Navigate to the "Sites" section.
    - Connect your repository or upload the `build` directory.
    - Ensure environment variables (PUBLIC_APPWRITE_ENDPOINT, PUBLIC_APPWRITE_PROJECT_ID, etc.) are set in the Sites configuration.
    - Provide the public URL once the site is live.
  </how-to-verify>
  <resume-signal>Type the public URL of the deployed site.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Final Performance & SEO Audit</name>
  <files>src/app.html, src/routes/+layout.svelte</files>
  <action>
    - Review `src/app.html` for any missing global meta tags (viewport, charset, etc.).
    - Verify that the sitemap and robots.txt are correctly served (implemented in Phase 3).
    - Run a local Lighthouse audit (if possible) or use a tool like PageSpeed Insights on the public URL.
    - Address any major performance bottlenecks (unused CSS, large assets).
  </action>
  <verify>
    <automated>npm run check</automated>
  </verify>
  <done>Site passes final quality checks with high performance and correct SEO metadata.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                     | Description                    |
| ---------------------------- | ------------------------------ |
| Public Web -> Appwrite Sites | SSL ensures encrypted traffic. |

## STRIDE Threat Register

| Threat ID | Category               | Component      | Disposition | Mitigation Plan                                                                                   |
| --------- | ---------------------- | -------------- | ----------- | ------------------------------------------------------------------------------------------------- |
| T-04-03   | Denial of Service      | Appwrite Sites | mitigate    | Appwrite's infrastructure handles basic DDoS; ensure no heavy server-side logic in public routes. |
| T-04-04   | Information Disclosure | Env Vars       | mitigate    | Ensure only `PUBLIC_` variables are exposed to the client.                                        |

</threat_model>

<verification>
- Site is reachable via public URL.
- Images load quickly and are optimized.
- Lighthouse scores are acceptable (90+).
</verification>

<success_criteria>

- MVP is live and functional.
- Performance optimized.
- Content seeding complete.
  </success_criteria>

<output>
After completion, create `.planning/phases/04-initial-content-seed-polish/04-02-SUMMARY.md`
</output>
