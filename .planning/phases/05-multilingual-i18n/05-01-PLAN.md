---
phase: "05"
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified: [
  "package.json",
  "src/lib/i18n.ts",
  "src/hooks.js",
  "src/hooks.server.js",
  "messages/en.json",
  "messages/pt-br.json",
  "project.inlang/settings.json"
]
autonomous: true
requirements: ["I18N-01", "I18N-02"]
must_haves:
  truths:
    - "Paraglide-js is initialized and generating code in src/lib/paraglide"
    - "Localized routing /[lang]/ is active and correctly rerouted via hooks"
    - "Basic UI messages are available in both PT-BR and EN"
  artifacts:
    - path: "src/lib/i18n.ts"
      provides: "i18n instance for SvelteKit"
    - path: "src/hooks.js"
      provides: "Reroute hook for localized URLs"
    - path: "src/hooks.server.js"
      provides: "Handle hook for locale detection"
  key_links:
    - from: "src/hooks.js"
      to: "src/lib/i18n.ts"
      via: "import and call i18n.reroute()"
---

<objective>
Setup the core i18n infrastructure using @inlang/paraglide-js and configure localized routing in SvelteKit.

Purpose: Enable multi-language support (PT-BR and EN) with SEO-friendly URLs.
Output: Paraglide initialized, SvelteKit adapter configured, and routing hooks implemented.
</objective>

<execution_context>
@/Users/me/Sites/astrobiologia/.gemini/get-shit-done/workflows/execute-plan.md
@/Users/me/Sites/astrobiologia/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-multilingual-i18n/05-RESEARCH.md
@src/lib/appwrite.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install and Initialize Paraglide-js</name>
  <files>package.json, project.inlang/settings.json, messages/en.json, messages/pt-br.json</files>
  <action>
    - Install @inlang/paraglide-js and @inlang/paraglide-sveltekit as dev dependencies.
    - Run `npx @inlang/paraglide-js@latest init` to initialize the project (select pt-br and en).
    - Configure `project.inlang/settings.json` to ensure correct source patterns and languages.
    - Create initial message files for `pt-br` and `en`.
    - Commit: `feat(i18n): install and initialize paraglide-js`
  </action>
  <verify>
    <automated>pnpm paraglide-js compile && ls src/lib/paraglide</automated>
  </verify>
  <done>Paraglide is initialized and generating translation functions in the expected directory.</done>
</task>

<task type="auto">
  <name>Task 2: Configure SvelteKit Localized Routing Hooks</name>
  <files>src/lib/i18n.ts, src/hooks.js, src/hooks.server.js</files>
  <action>
    - Create `src/lib/i18n.ts` to export the paraglide-sveltekit `i18n` instance.
    - Implement the `reroute` hook in `src/hooks.js` using `i18n.reroute()`.
    - Implement the `handle` hook in `src/hooks.server.js` using `i18n.handle()` to detect and set the locale on the server.
    - Ensure `src/hooks.server.js` correctly wraps any existing handle logic.
    - Commit: `feat(i18n): configure localized routing hooks`
  </action>
  <verify>
    <automated>pnpm check</automated>
  </verify>
  <done>SvelteKit is configured to handle /[lang]/ routes via paraglide-sveltekit.</done>
</task>

<task type="auto">
  <name>Task 3: Define Initial UI Messages</name>
  <files>messages/en.json, messages/pt-br.json</files>
  <action>
    - Add common UI keys to `messages/pt-br.json` (Header, Footer, common buttons).
    - Add matching keys to `messages/en.json`.
    - Example keys: `site_title`, `nav_home`, `nav_articles`, `nav_about`, `footer_tagline`.
    - Run paraglide compile to update generated code.
    - Commit: `feat(i18n): define initial UI messages for PT-BR and EN`
  </action>
  <verify>
    <automated>pnpm paraglide-js compile</automated>
  </verify>
  <done>Basic UI messages are defined and accessible via paraglide functions.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| URL -> Server | Untrusted locale parameter in the URL. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | Tampering | hooks.js | mitigate | Paraglide-sveltekit validates the locale against allowed list automatically. |
| T-05-02 | DoS | hooks.server.js | mitigate | Simple locale detection logic has negligible performance impact. |
</threat_model>

<verification>
- Run `pnpm dev` and visit `/en/` and `/pt-br/` to ensure no 404s.
- Verify `src/lib/paraglide/runtime.js` exists.
</verification>

<success_criteria>
Paraglide-js is fully integrated with SvelteKit, and localized routes are correctly handled by the framework.
</success_criteria>

<output>
After completion, create `.planning/phases/05-multilingual-i18n/05-01-SUMMARY.md`
</output>
