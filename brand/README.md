# Brand

Design tokens and visual identity live in the canonical repo:

→ [brand_identity_danielvm](https://github.com/danielvm-git/brand_identity_danielvm)

This directory is a pointer, not a copy. To pull design tokens into this project:

```bash
# From brand_identity_danielvm
cp brand/tokens.css /path/to/astrobiologia/src/styles/
```
